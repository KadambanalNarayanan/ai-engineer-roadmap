"""
Without running it, predict the output:

numbers = [10, 20]

def replace_list(lst):
    lst = [100, 200]
    print("Inside:", lst)

replace_list(numbers)

print("Outside:", numbers)


What is printed inside the function?
What is printed outside the function?
Why didn't the original list change this time, even though lists are mutable?

"""

